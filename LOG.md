# 工程日志

记录每次改动**为什么这么改、踩了什么坑、核查抓到什么**。不写密钥。
新条目加在最上面。

---

## 2026-08-08（二）　全链路端到端自检：三方审查在上生产前拦下一次假绿灯

**起因**：所有者问「家长下单→老师接单付费→企微收到通知，这一圈都跑过了吗」。**答案是没有**——之前只用后台 admin 的四个 action 验过结算金额，教师端/家长端的真实操作路径没走过，企微通知一条都没实际触发过（本地没配 `WECOM_WEBHOOK_URL`，`notify.ts` 直接 return）。

**关键点：脚本写完先送 ds-review / Codex / Kimi 三审，结果被判定不该上生产。** 抓到两个致命错误，都是"接口调用方式写错，但断言仍会打勾"：

1. **`GET /api/teacher/profile` 必须带 `?id=<teacherId>`**，v1 没传 → 接口返回 400 → 而检查逻辑是"响应体里有没有出现家长手机号"，400 的错误响应里当然没有 → **假通过**。等于平台命根子那道验证完全失效，还会给一个绿灯。
2. **`PATCH /api/admin/matches/[id]` 必须带 `{action:'confirm_payment'}`**，v1 传了 `{}` → 400 → 收款根本没确认 → 后续"付费后能看到联系方式"连带失真。

三方还共同指出：`bad()` 不中止流程，前置失败后会拿 `undefined` 继续撞生产接口（`/api/admin/matches/undefined`）；固定测试手机号 + `.single()` 定位可能命中真实数据，清理时误删真实老师；Supabase 的 `{error}` 不检查就打印成功；`try` 里抛异常时 `finally` 仍打印"✅ 全链路通过"。

**Kimi 独有的一条最有价值**：v1 用 service_role 直接 `update(teachers)` 做"上架"，它指出应该走真实接口。查证后确实存在 `PATCH /api/admin/teachers/[id]`（教务审核改 tier/is_visible）和 `PUT /api/teacher/profile`（老师自己填资料）。改成走这两个真实接口后，service_role 只剩读核对和清理，**完全符合铁律 4.1**，还额外验证了 mass assignment 防护（老师提交 `{tier:3, is_visible:true, email:...}` 应该被白名单挡掉）。

**推翻一条**：Kimi 说 `keys.includes('email')` 只看字段名、值为 null 也会误报。但 `/api/teachers` 的 select 白名单里压根没有 `email`/`phone`，这个 key 出现本身就说明白名单被改坏了，按 key 判断更严格，保留。

**先本地后线上**：定稿后先 `E2E_BASE=http://localhost:3000` 跑（本地没配 webhook，不打扰工作群），当场又抓到一个 bug——**测试手机号生成少一位**（`1350009` 是 7 字符 + 3 位随机 = 10 位），注册接口按 `/^\d{11}$/` 校验直接 400。真打线上就白费一次请求。修完本地全绿，再打线上。

**线上结果：14 步 39 项全通过**，含：
- 🔒 未确认收款时，老师端接口里家长手机号/微信被服务端置空；🔓 确认收款后拿得到
- 🔒 老师改不了自己的档位/上架状态/登录邮箱（mass assignment 防护）
- 课时状态正确流转 `pending → teacher_done → confirmed`
- 结算 ¥200 → 平台抽成 ¥0、老师全额拿到
- 公开接口无 email/phone，家长端看不到 `platform_fee`/`settle_amount`/`platform_rate`/`notes`

跑完数据清干净（3 位老师、2 个 auth 账号、三张业务表 0 行，与开工前一致）。脚本收进 `scripts/e2e-live-check.mjs`。

**企微通知已由所有者在群里确认收到全部 3 条**（新老师注册 / 新预约 / 老师接单付费）——这是脚本验不了的唯一一环，至此整条链路闭合：**家长端和教师端现在都可以正常使用，可以放心让真老师进来填资料、真家长下单。**

**教训**：**验证脚本本身也会有 bug，而且它的 bug 特别毒——会把"没验到"伪装成"验过了"。** 这次两个致命问题都不是"跑不起来"，而是"跑得很顺、全打勾、但验的根本不是那回事"。危险操作上生产前先三审，成本远低于拿一个假绿灯去做决策。

---

## 2026-08-08　补上缺失的建表 SQL，并在临时项目真跑了一次清空脚本

**起因**：清空脚本 `--execute` 那条路径一直没被执行过——拿正式库测不可能，一跑三条老师数据就没了。所以开了个一次性的 Supabase 免费项目实跑。

**卡在第一步：仓库里根本没有主表的建表 SQL。** `supabase/` 只有 `password_reset_requests` 和未启用的 `teacher_reviews`，`teachers`/`bookings`/`matches`/`lesson_orders` 四张主表的 DDL 从来没进过版本库。接手的人没法从零搭环境，我也没法复刻。已从线上库反推补齐成 `supabase/schema.sql`。

**反推方式**：在 SQL Editor 里查 `pg_class`/`pg_attribute`/`pg_constraint`/`pg_indexes`，拼出列定义、约束、索引。

**两个浏览器操作上的坑**：
- Supabase 的 SQL Editor 是 **Monaco** 不是 CodeMirror，Playwright 的 `fill` 填不干净（新内容写进去了，旧内容比新内容长的尾巴会留下，两条 SQL 拼在一起）。改用 `window.monaco.editor.getModels()[0].setValue()` 一次性替换，干净可靠。
- 查询里只要出现 `create table` 字样（哪怕只是字符串拼接、根本不建表），Supabase 就弹 RLS 确认框拦住。把字面量拆成 `'crea'||'te table'` 可绕过；真建表时选「Run without RLS」（线上也没配 RLS，权限全靠服务端 service_role）。

**查 schema 时顺带发现的事实**：
- 外键除 `password_reset_requests.teacher_id` 外**都没有 ON DELETE CASCADE**，所以删除顺序错了会直接撞外键报错——这次是硬验证，不是推演。脚本的顺序 `lesson_orders → matches → password_reset_requests → bookings → teachers` 正确。
- `bookings.teacher_id` 也有外键指向 `teachers`，之前没意识到。
- ⚠️ `payment_status` 的 CHECK 允许 `pending/paid/**refunded**`，但 `lib/types.ts` 只声明了 `'pending' | 'paid'`。退款这条路数据库开着、代码没实现。已记进 HANDOFF。
- `platform_rate` 默认值确认是 `0`，昨天那条 SQL 生效了。

**给脚本加了 `--env-file`**：原本硬读 `.env.local`（正式库），导致它**根本没法被测试**。加了这个参数才能指向临时项目。同时在输出里打印「配置来源」，免得哪天连错库还不知道。

**验证结果**：在临时项目灌了带完整外键关联的假数据（3 老师 / 2 需求 / 2 匹配 / 2 订单 / 1 改密申请 / 3 个 Auth 账号含一个孤儿 / 1 个头像），跑 `--execute` 全流程通过。**没只信脚本自己的输出**，另写脚本独立复核：5 张表全空、`auth.users` 0 个、`avatars` 0 个文件、`teachers` 仍能正常插入（结构完好）、`lesson_status` 的 CHECK 约束仍拒绝非法值。

跑完确认正式库分毫未动（三条老师、两个 Auth 账号都在），临时项目已删除，本地临时文件和假数据备份也清了。

**教训**：一次性的危险脚本，"写好了但没跑过"和"跑过了"是两回事。开个免费临时项目实跑一遍的成本，比真到那天出事低得多。

---

## 2026-08-07（二）　招募漏斗排查 + 加网课支持

**怎么查的**：以老师身份在手机尺寸（390×844）真走了一遍 注册 → 登录 → 教师端填资料，不是读代码猜。测试账号和数据每轮都清干净了。

**查出 6 个问题，最严重的是「招募页承诺了系统兑不了」——和抽成那件事同一类：**

### 🔴 1. 承诺「可以只上网课」，但系统根本没有网课

`app/laoshi/page.tsx` 的 FAQ 写着授课方式有「上门 / 到访 / 网课」，而 `lib/types.ts` 里是 `'上门' | '工作室' | '均可'`——**三个词一个都对不上**，网课在类型层面就不存在。老师冲着能上网课注册进来，填资料时发现选不了。

所有者决策：**加网课支持**（不是改文案）。理由是网课能把可招老师的范围从南京扩到全国，而现在唯一的瓶颈就是老师太少（2 位可见、都是语文）。

改动：`teaching_mode` 加 `'网课'`；教师端四个按钮；家长端加「方式」筛选（可上门 / 支持网课 / 去工作室）；FAQ 改成跟实际选项一致的措辞。

**实测数据库这列没有 CHECK 约束**（插临时行探的，已删），所以加新值不用先跑 SQL——和 `lesson_status` 那个坑不一样，那个有约束。

**顺手修掉一个反向条件**：`components/TeacherCard.tsx` 原本写 `teaching_mode !== '工作室'` 就显示「上门范围」。加了网课以后，纯网课老师会被这个反向条件归进"要显示上门范围"那一类。已改成正向列举。**教训：枚举值的条件判断一律正向列举，别写 `!==`，否则每加一个新值都会悄悄归错类。**

### 🟡 2. 教师端所有页面顶着家长端的标题

只有 `/laoshi` 有自己的 metadata，注册页/登录页/教师端全都继承根 layout 的「家师有约 — 严选南京家教」和面向家长的分享卡片。老师把注册链接转到同行群，卡片在向家长打广告。

加了 `app/teacher/layout.tsx`（教师端通用）、`app/teacher/register/layout.tsx`（转发最多的一页）、`app/teacher/dashboard/layout.tsx`。

**坑：Next.js 的 metadata 在路由段之间是浅合并，不是深合并。** 子段写了 `openGraph` 就会把父段那份整个替换掉。第一版只写了 title/description，结果教师端所有页面的分享卡片**没图**了（根 layout 的 `images` 被顶掉）。现在每一层的 `openGraph` 都写全 type/siteName/locale/images。

**robots 用「默认关 + 白名单开」**：`app/teacher/layout.tsx` 默认 `noindex`（教师端多是登录后或敏感流程页），只有 `/teacher/register` 在自己的 layout 里显式开 `index`。反过来做（默认开、逐个关）以后加新页面必漏。

### 🟡 3. 注册成功不自动登录

注册完直接踢回登录页，要求把刚设的密码再输一遍——招募漏斗最后一步的经典流失点。改成复用 `/api/auth/login` 拿 token 自动登录，进 `dashboard?welcome=1`。

**这段的 try/catch 必须独立**（Codex 和 ds-review 两个审查器独立抓到同一个 bug）：自动登录的 fetch 如果因断网/超时 reject，会落到外层 catch 显示「注册失败」——可账号其实已经建好了，老师以为没成功再注册一次，撞上「该手机号已注册」直接卡死在门口。现在自动登录出任何差错都只是回退到登录页。

### 🟡 4. 期望落差：招募页说「填 3 项，1 分钟」，进门看到 10 项

注册确实只要 3 项，但一进教师端资料完成度 0%、一次性列出 10 项待填。改成两档：**核心 5 项**（科目/年级/课时费/上课方式/可上课时间，教务匹配必需）+ **加分 5 项**（身份类型/教龄/亮点/介绍/头像，影响家长选中率，之后再补也行）。

### 🟠 5. 「一句话亮点」的 placeholder 有法律风险

原文是「连续3年送学生语文成绩提升20分」。《广告法》24 条禁止教育培训广告对升学、考试成绩作保证性承诺，而 `highlight` 字段**直接展示在家长端**——等于平台在教老师写违规文案。已改成资历式示例，并加了提示。档位说明里的「有稳定的提分案例」也一并软化。

**⚠️ 存量数据仍有问题，代码改不掉**：线上两位可见老师的 `highlight` 都是效果承诺式写法（「多名学生中考语文120+」「将他们的语文成绩提升20+，有几个学生是语文年级第一」），`bio` 里还有「所带学生有29中、钟英中学、南外、玄外、九中等名校学生」。**这些正在线上展示给家长，需要所有者自己去后台改文案**，见 HANDOFF 待办。

### 6. 存量 3 位老师的 `phone` 全是 null

07-29 加了 phone 列并修了注册丢手机号的 bug，但存量老师没回填。真要联系她们手上没号码。

### 顺带修的（审查器提的，复核后确认）

- `app/api/auth/login/route.ts` 用 `select('*')` 把整条 teacher 记录返回浏览器。**复核结论：当前没有实际泄露**——`teachers` 表 20 列全是老师本人资料，没有后台内部备注字段。但铁律 4.2 明令不许 `select('*')`，且以后加任何内部字段都会自动泄露，07-29 刚因为这个泄露过老师邮箱。已收紧到实际用到的 `id, name`（两个调用点都只用这两个）。

**三方对抗性检查**：ds-review（DeepSeek）、Codex、Kimi。ds-review 和 Codex 独立抓到同一个自动登录 bug，Codex 另外抓到 metadata 浅合并和 robots 覆盖不全。

---

## 2026-08-07　定价决策：去掉 8% 课时费抽成，文案和实现对齐

**决策**：招募页四处承诺「平台不从课时费里抽点」，结算却实扣 8%。所有者拍板**以文案为准，改结算**。
理由：现在唯一的瓶颈是招不到老师（上线 4 个月 0 单、2 位可见老师），「不抽课时费」是招募页最硬的卖点，此时丢掉它得不偿失；而且信息费（接单时收一节课时费）本来就是一道收费点，抽成是第二道。代价是每单少 8%，但 8% × 0 单 = 0。

**核查时发现文案不止三处，是四处**：`app/laoshi/page.tsx` 的 `:6` metadata description、`:22-23` 卖点卡片、`:48` FAQ、`:91` 首屏标语。HANDOFF 之前记的「三处」漏了 metadata。文案全部原样保留，一个字没改。

**顺手抓到一个真 bug**：`app/teacher/dashboard/page.tsx` 用 `Number(l.platform_rate || 0.08)`——`||` 会把 `0` 当假值吃掉。也就是说**哪怕把数据库里的 rate 全改成 0，老师账单页照样显示扣 8%**，改结算等于白改。同一份数据后台页用的却是 `?? 0.08`（能正确认 0），两边行为不一致。已统一成 `?? 0`。

**教训**：数值型配置的兜底默认值只能用 `??` 不能用 `||`，因为 0 是合法取值。

**改动**：
- `app/api/admin/lessons/[id]/route.ts` 结算 rate 兜底 `?? 0.08` → `?? 0`
- `app/api/admin/lessons/route.ts` 建单显式写 `platform_rate: 0`，不依赖数据库列默认值
- `app/teacher/dashboard/page.tsx` 修 `||` bug；抽成为 0 时不渲染「平台抽成」行（别让老师看到「平台抽成 ¥0.00」平白生疑）；未结算订单上**硬编码的「（抽8%）」**改成按 rate 动态显示
- `app/admin/lessons/page.tsx` 同样 `?? 0`，抽成为 0 时不显示抽成预览和结算明细里的平台那一段
- `app/admin/page.tsx` 看板说明「平台收入 = 课时费 × 8%」是错的了，改成如实说明；指标名改为「本月课时费抽成」
- 新增 `supabase/platform_rate_default_0.sql`（**待手动执行**）

**收尾（当天晚些时候）**：`supabase/platform_rate_default_0.sql` 已在 Supabase SQL Editor 执行。执行前查 `information_schema` 是 `0.08`，执行后是 `0`；又用「绕过 API 直接 POST 到 REST、不指定 platform_rate」实测了一次，拿到 0（这正是这条 SQL 要防的场景）。探测行已删，`lesson_orders` 仍是 0 行。

**探测到的坑：数据库列默认值是 0.08，光改代码不够。** 建表 SQL 不在仓库里，建单接口也不写 `platform_rate`，全靠列默认值。用一条临时插入探明默认值确实是 0.08（探测行已删，表回到 0 行）。所以两手都做了：代码建单显式写 0（走 API 的单立即生效，不用等你跑 SQL），SQL 文件改列默认值（兜底手工插行的情况）。

**留下的窟窿（已记进 HANDOFF 待办 1）**：抽成去掉后，平台收入**没有任何地方记账**了。`matches` 表只有 `payment_confirmed` 布尔，不存信息费金额，所以后台看板「本月课时费抽成」从此恒为 0，而真正的收入（信息费）根本不在系统里。有第一笔真实收入之前不急着做。

**验证**：`npm run build` 通过；查线上库 `lesson_orders` 0 行、`matches` 0 行，无存量数据需要迁移。

---

## 2026-08-01　评价系统：设计完成，决定暂不上线

**做了什么**：用三个独立视角（产品 / 技术 / 风险）评估「家长给老师评价打分」功能，产出完整设计后**决定暂缓**。

**结论：现在不做公开评分。** 三条决定性理由：

1. **老师能给自己刷好评，且防不住。** `app/api/teacher/lessons/route.ts` 的返回字段里同时有订单 `id` 和 `parent_phone`——而家长端评价的全部凭证就是这两样（家长没有账号）。老师刷的假好评会挂在真实已完课订单上、带"已验证"标记，和真评价一个字都分不出来。这不是防刷做得不够，是现有凭证体系下根本防不住。
2. **法律暴露面。** 《电子商务法》39 条禁止平台删除消费者评价，但老师是付费方一定会要求删；《民法典》1195 条下不及时处理失实评价平台担连带责任。两头夹击。且站点目前没有用户协议、隐私政策、举报入口。家长评论几乎必然带未成年人信息（学校、年级、上门地址）。
3. **供给侧反噬 + 样本量陷阱。** 平台仅 2 位可见老师、0 单，正处在求老师进来的阶段，此时给付费方加公开声誉风险是负筹码。且 1 条差评能把均分砸穿。

**改为先做**（尚未实现）：私密回访（家长确认完课后弹满意度，只进后台+推企微，不公开不算分）+ 老师卡片放客观计数（已完成 N 节课 / 服务 N 个家庭）。

**留档**：`supabase/teacher_reviews.sql` 是完整建表设计稿，**未执行、未启用**。等四个条件满足再用：≥15 位真实第三方老师 / 每位老师 ≥5 条已完课订单 / 月订单 20+ / 家长端凭证改成一订单一随机码（且随机码不能出现在老师端接口）。

**顺带发现的两个独立问题**：
- ⚠️ **已于 2026-08-07 解决（见本文件最上面那条）**：`app/laoshi/page.tsx` 承诺「平台不从课时费里抽点」，但 `app/api/admin/lessons/[id]/route.ts` 结算实扣 8%，且老师在教师端账单页看得见 `platform_fee`。**文案和实现必须对齐一个**，这是待定价决策。
- ✅ 已修：`app/api/parent/lessons/[id]/confirm/route.ts` 是家长端唯一的写接口，07-29 加限流时漏了，已补（`e09f0e5`）。

**核查发现的前置改动**：`components/TeacherCard.tsx` 的档位标签本身就是星星（⭐基础档/⭐⭐进阶档/⭐⭐⭐精英档）。**评分上线前必须先把 tier 换成文字或色块**，否则家长会把「⭐⭐ 进阶档」读成"2 星差评"。

---

## 2026-07-30　接通企业微信通知

**做了什么**：家长提交预约 / 老师接单付信息费 / 新老师注册，三种事件推送到企业微信群。

**坑 1：新版企业微信把「群机器人」改名叫「消息推送」**。路径是 群聊 → 右上 `⋯` → 聊天信息 → 消息推送 → 添加。全员群里找不到「群机器人」字样，容易误判为不支持。另外**机器人不会作为群成员出现**在右侧名单里，判断通没通只看消息进没进群。

**坑 2：手机号打码把 Markdown 撑坏了**（`20de53d`）。企业微信按 Markdown 解析消息，`139****0000` 里那四个星号被当成加粗标记吃掉，导致后面「学生情况」等字段的 `**加粗**` 全部错位。改为完整显示手机号——这是教务自己的通知群，看到号码能直接打电话更实用。

**坑 3：Mac 原生应用自动化**。Playwright 只能驱动浏览器。企业微信客户端自绘 UI 不给辅助功能提供任何名称（127 个元素全是无名 button），只能截图看坐标点。且 `System Events` 的合成点击会被忽略，必须用 `cliclick` 真实移动光标。中文输入会被拼音输入法截走，要走剪贴板 + Cmd+V。

**验证**：走完整链路——调 `/api/bookings` → 数据进库 → 服务端推企微 → 截图确认消息进群。测试数据已清理。

---

## 2026-07-29　大修一轮（21 个提交）

上线 4 个月首次系统性检查，用 Codex + Kimi 做三方对抗性审查，抓到多个从没被发现的问题。

### 🔴 最严重：信息费模式形同虚设（`b953b69`）

`/api/teacher/profile` 用 `bookings(*)` 把家长 `phone`/`wechat` **全量返回**给老师，前端只是在 `payment_confirmed=false` 时不渲染。老师按 F12 看 Network、或直接 curl 接口，**不接单不付费就能拿到家长联系方式**，平台的收费点等于不存在。实测造数据坐实后修复：服务端在未确认收款时把两个字段置空。付费后能看到、未付费看不到，均已验证。

**教训**：敏感字段只在前端隐藏等于没隐藏。

### 🔴 课时系统状态机是坏的（`3f481da`）

数据库 CHECK 约束只认 `pending/teacher_done/confirmed/auto_confirmed/cancelled`，代码却一路写死 `'completed'`。老师一点「标记完课」就被数据库拒绝——**从 4 月上线到 7 月，完课往后整条结算链路根本走不通**，只因一直 0 单没人踩到。新增 `lib/lesson-status.ts` 统一状态取值。

**教训**：状态值必须收在一个文件里，别散在各处写字符串。

### 🔴 后台密码明文在公开仓库（`022f0ab` `6591052`）

兜底密码 `jiashi2026` 硬编码在代码里，而仓库是 PUBLIC 的——任何人都能登进后台看所有家长手机号。改为只读 `ADMIN_PASSWORD` 环境变量 + `timingSafeEqual` 常量时间比较；Vercel 上换成随机强密码、删掉会被打进前端 bundle 的 `NEXT_PUBLIC_ADMIN_PASSWORD`。旧密码实测已失效。

### 其它安全修复

- **老师能自己升档上架**：`/api/teacher/profile` PUT 是 mass assignment，提交 `{tier:3, is_visible:true}` 就能绕过审核。改字段白名单。
- **公开接口泄露老师邮箱**：`/api/teachers` 用 `select('*')`。改白名单。
- **并发重复结算**（Kimi 抓到，Codex 漏了）：状态流转全是「读→判断→写」，两个并发请求会同时通过判断都写入。改成带前置条件的 UPDATE，没命中行返回 409。实测 6 个并发结算请求只有 1 个成功。
- **cancelled 订单能被复活**：`!isDone()` 判断把已取消订单也放行了。改为只有 `pending` 能标完课。
- 家长端不再下发 `settle_amount`/`platform_fee`/`platform_rate`/`notes`。

### 招募漏斗修通

**老师根本找不到入口**——首页没有任何教师入口，注册页只能从登录页底部进，而登录页也没人链接过去。唯一途径是所有者亲口发网址。新增 `/laoshi` 招募页（`teacher.jiashiyouyue.com` 根路径重写到这里）、首页页脚入口。

**注册时填的手机号直接丢了**：前端传了 `phone`，但 API 只解构 `{name, email, password}`，而且 `teachers` 表压根没有 `phone` 列。招来人却联系不上。已加列 + 存值 + 后台显示。注册表单从 4 个字段（含"邮箱手机二选一"）简化到 3 个。

**坑：Next.js rewrite 要放 `beforeFiles`**（`8dafa56`）。`rewrites()` 直接返回数组时规则在匹配完文件系统之后才跑，`/` 命中真实存在的 `app/page.tsx`，按 host 的重写永远不触发。排查靠响应头 `x-matched-path`。

### 其它

- 限流（`lib/rate-limit.ts`）、超时自动确认定时任务（`vercel.json` cron）、课时列表分页、后台数据看板 `/admin`、教师端资料完成度提示
- 老师须知正文移到 `/api/teacher/rules` 服务端校验后返回，不再编译进公开 JS bundle
- 微信分享卡片 + 站点图标（**微信会把卡片图居中裁成正方形**，文字必须收在图中间的正方形区域内）
- Supabase 免费版被暂停过一次（超 7 天不活跃），已恢复

### 已知并接受的设计

**家长端只用手机号做凭证**，知道手机号就能查订单、替家长确认完课。所有者已知晓并决定保持——目前老师少且都认识，国内短信服务要企业营业执照，成本不划算。**不要自作主张改**。
